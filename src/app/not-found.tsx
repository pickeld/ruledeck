export default function NotFound() {
  return (
    <main className="grid min-h-full place-items-center px-6">
      <div>
        <p className="font-display text-3xl">Not found</p>
        <p className="mt-2 text-sm text-muted">That project or artifact is not in this deck.</p>
      </div>
    </main>
  );
}
